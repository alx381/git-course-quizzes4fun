# 🧩 Git для новичков: пошаговая инструкция

## 1. Что такое Git и зачем он нужен

**Git** — это система контроля версий. Она позволяет:

* сохранять историю изменений файлов;
* работать над проектом в команде;
* откатываться к предыдущим версиям;
* экспериментировать, не ломая основной код.

📌 Проще говоря: Git — это «умное сохранение» проекта.

---

## 2. Установка Git

### Windows

1. Скачайте Git: [https://git-scm.com/downloads](https://git-scm.com/downloads)
2. Установите, оставляя настройки по умолчанию
3. После установки откройте **Git Bash**

### macOS

```bash
brew install git
```

Или установите через Xcode Command Line Tools.

### Linux

```bash
sudo apt install git
```

Проверьте установку:

```bash
git --version
```

---

## 3. Первичная настройка Git

Укажите имя и email (используются в истории коммитов):

```bash
git config --global user.name "Ваше Имя"
git config --global user.email "email@example.com"
```

Проверить настройки:

```bash
git config --list
```

---

## 4. Основные понятия Git

| Термин         | Описание                               |
| -------------- | -------------------------------------- |
| Репозиторий    | Проект под управлением Git             |
| Коммит         | Сохранение изменений                   |
| Ветка (branch) | Отдельная линия разработки             |
| main / master  | Основная ветка                         |
| remote         | Удалённый репозиторий (GitHub, GitLab) |

---

## 5. Создание репозитория

### Новый проект

```bash
git init
```

### Клонирование существующего проекта

```bash
git clone https://github.com/user/project.git
```

---

## 6. Жизненный цикл файлов в Git

```text
Изменён → добавлен (staging) → закоммичен
```

Проверить статус:

```bash
git status
```

---

## 7. Добавление файлов в коммит

Добавить один файл:

```bash
git add file.txt
```

Добавить все файлы:

```bash
git add .
```

---

## 8. Создание коммита

```bash
git commit -m "Краткое описание изменений"
```

Рекомендации:

* коммит = одна логическая задача
* сообщение должно описывать **что сделано**

---

## 9. Просмотр истории коммитов

```bash
git log
```

Краткий формат:

```bash
git log --oneline
```

---

## 10. Работа с ветками

Показать ветки:

```bash
git branch
```

Создать ветку:

```bash
git branch feature-login
```

Переключиться:

```bash
git checkout feature-login
```

Создать и переключиться:

```bash
git checkout -b feature-login
```

---

## 11. Слияние веток

Перейти в основную ветку:

```bash
git checkout main
```

Слить изменения:

```bash
git merge feature-login
```

---

## 12. Удалённые репозитории

Проверить remote:

```bash
git remote -v
```

Добавить remote:

```bash
git remote add origin https://github.com/user/project.git
```

---

## 13. Отправка изменений на сервер

```bash
git push origin main
```

Первый push:

```bash
git push -u origin main
```

---

## 14. Получение изменений с сервера

```bash
git pull
```

---

## 15. Отмена изменений

Отменить изменения в файле:

```bash
git checkout -- file.txt
```

Убрать файл из staging:

```bash
git reset file.txt
```

Отменить последний коммит (без потери файлов):

```bash
git reset --soft HEAD~1
```

---

## 16. Файл .gitignore

Пример:

```text
node_modules/
.env
*.log
```

---

## 17. Частые ошибки новичков

* забыли `git add`
* коммиты без смысла
* работа напрямую в `main`
* отсутствие коммитов перед `pull`

---

## 18. Шпаргалка команд

```bash
git init
git clone
git status
git add .
git commit -m "msg"
git branch
git checkout
git merge
git pull
git push
```

---

## 19. Рекомендации

* Делайте коммиты часто
* Используйте ветки
* Проверяйте `git status`
* Не бойтесь Git — он почти всё прощает
